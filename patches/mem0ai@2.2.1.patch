diff --git a/node_modules/mem0ai/.bun-tag-40f46e0fa24dff6e b/.bun-tag-40f46e0fa24dff6e
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/oss/index.js b/dist/oss/index.js
index d23ec7c2b51b801586307e16ea3e4a7456c75cf0..5089433bcf1d9395fd3ad0e641d23b4d693ce3d8 100644
--- a/dist/oss/index.js
+++ b/dist/oss/index.js
@@ -122,7 +122,10 @@ var MemoryConfigSchema = import_zod.z.object({
 var import_openai = __toESM(require("openai"));
 var OpenAIEmbedder = class {
   constructor(config) {
-    this.openai = new import_openai.default({ apiKey: config.apiKey });
+    this.openai = new import_openai.default({
+      apiKey: config.apiKey,
+      baseURL: config.baseURL || config.url
+    });
     this.model = config.model || "text-embedding-3-small";
     this.embeddingDims = config.embeddingDims || 1536;
   }
@@ -469,7 +472,6 @@ var MistralLLM = class {
 };
 
 // src/oss/src/vector_stores/memory.ts
-var import_sqlite3 = __toESM(require("sqlite3"));
 var import_path = __toESM(require("path"));
 var MemoryVectorStore = class {
   constructor(config) {
@@ -478,7 +480,17 @@ var MemoryVectorStore = class {
     if (config.dbPath) {
       this.dbPath = config.dbPath;
     }
-    this.db = new import_sqlite3.default.Database(this.dbPath);
+    let sqlite3;
+    try {
+      sqlite3 = __toESM(require("sqlite3"));
+    } catch (error) {
+      const message = error instanceof Error ? error.message : String(error);
+      throw new Error(
+        `Failed to load sqlite3: ${message}\n` +
+          "Make sure sqlite3 is installed"
+      );
+    }
+    this.db = new sqlite3.default.Database(this.dbPath);
     this.init().catch(console.error);
   }
   async init() {
@@ -2071,10 +2083,19 @@ See the SQL migration instructions in the code comments.`
 };
 
 // src/oss/src/storage/SQLiteManager.ts
-var import_sqlite32 = __toESM(require("sqlite3"));
 var SQLiteManager = class {
   constructor(dbPath) {
-    this.db = new import_sqlite32.default.Database(dbPath);
+    let sqlite3;
+    try {
+      sqlite3 = __toESM(require("sqlite3"));
+    } catch (error) {
+      const message = error instanceof Error ? error.message : String(error);
+      throw new Error(
+        `Failed to load sqlite3: ${message}\n` +
+          "Make sure sqlite3 is installed"
+      );
+    }
+    this.db = new sqlite3.default.Database(dbPath);
     this.init().catch(console.error);
   }
   async init() {
diff --git a/dist/oss/index.mjs b/dist/oss/index.mjs
index 5b7f57a851f56eca4aaa6a468f84a912a6f73f88..03167d846c86c126574f5d509b13772269433980 100644
--- a/dist/oss/index.mjs
+++ b/dist/oss/index.mjs
@@ -1,6 +1,9 @@
 // src/oss/src/memory/index.ts
 import { v4 as uuidv43 } from "uuid";
 import { createHash } from "crypto";
+import { createRequire } from "module";
+
+const require = createRequire(import.meta.url);
 
 // src/oss/src/types/index.ts
 import { z } from "zod";
@@ -61,7 +64,10 @@ var MemoryConfigSchema = z.object({
 import OpenAI from "openai";
 var OpenAIEmbedder = class {
   constructor(config) {
-    this.openai = new OpenAI({ apiKey: config.apiKey });
+    this.openai = new OpenAI({
+      apiKey: config.apiKey,
+      baseURL: config.baseURL || config.url
+    });
     this.model = config.model || "text-embedding-3-small";
     this.embeddingDims = config.embeddingDims || 1536;
   }
@@ -408,7 +414,6 @@ var MistralLLM = class {
 };
 
 // src/oss/src/vector_stores/memory.ts
-import sqlite3 from "sqlite3";
 import path from "path";
 var MemoryVectorStore = class {
   constructor(config) {
@@ -417,6 +422,16 @@ var MemoryVectorStore = class {
     if (config.dbPath) {
       this.dbPath = config.dbPath;
     }
+    let sqlite3;
+    try {
+      sqlite3 = require("sqlite3");
+    } catch (error) {
+      const message = error instanceof Error ? error.message : String(error);
+      throw new Error(
+        `Failed to load sqlite3: ${message}\n` +
+          "Make sure sqlite3 is installed"
+      );
+    }
     this.db = new sqlite3.Database(this.dbPath);
     this.init().catch(console.error);
   }
@@ -2010,10 +2025,19 @@ See the SQL migration instructions in the code comments.`
 };
 
 // src/oss/src/storage/SQLiteManager.ts
-import sqlite32 from "sqlite3";
 var SQLiteManager = class {
   constructor(dbPath) {
-    this.db = new sqlite32.Database(dbPath);
+    let sqlite3;
+    try {
+      sqlite3 = require("sqlite3");
+    } catch (error) {
+      const message = error instanceof Error ? error.message : String(error);
+      throw new Error(
+        `Failed to load sqlite3: ${message}\n` +
+          "Make sure sqlite3 is installed"
+      );
+    }
+    this.db = new sqlite3.Database(dbPath);
     this.init().catch(console.error);
   }
   async init() {
